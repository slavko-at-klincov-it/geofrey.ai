# Ralph Wiggum Prompt — geofrey.ai Build

> **Anleitung:**
> 1. Starte: `cd ~/Code/openClawNurBesser && claude --dangerously-skip-permissions`
> 2. Kopiere den Prompt aus `RALPH_PROMPT_SHELL_SAFE.txt`
> 3. Führe aus: `/ralph-loop "$(cat RALPH_PROMPT_SHELL_SAFE.txt)" --completion-promise "GEOFREY_BUILD_COMPLETE" --max-iterations 40`
>
> Falls das auch nicht geht, einfach den Inhalt von RALPH_PROMPT_SHELL_SAFE.txt direkt als Nachricht einfügen (ohne /ralph-loop)