---
project: geofrey
session_id: 691d7f6f-ce4a-4d2d-8002-1022eaa44014
session_date: "2026-03-13"
extracted_at: "2026-03-24T18:23:10"
content_hash: 72910612ae456bbd
---

# Session Learnings: geofrey (2026-03-13)

## Decisions
- Implement `ConsForLlm` function to handle nested objects for security compliance
- Run `pnpm setup` to execute the interactive configuration wizard
- Google OAuth keys are required for cloud features but can be omitted for local providers
- Hybrid Risk Classification: Combine 90% Regex rules with 10% LLM verification to minimize latency while maintaining security
- Adopt 'Static Analysis' pattern: Replace runtime heuristics with local environment inspection (e.g., `osascript`) to validate context without network calls
- Prioritize proactive morning briefings as the #1 requested market feature, validated by research and user preference for AI agents that initiate summaries.
- Implement local-first architecture using Qwen3 8B via Ollama to ensure data privacy, prevent cloud API leakage, and satisfy the refusal of cloud AI by 78% of potential users.
- Adopt the Vercel AI SDK v6 `ToolLoopAgent` pattern to orchestrate tool usage and manage sequential tool interactions effectively.
- Enforce a hybrid risk classification strategy combining 90% deterministic regex matching against 42 Level 3 patterns with a 10% LLM fallback for low-confidence edge cases.
- Enforce a promise-based approval gate mechanism to block all Level 2 actions until explicit user confirmation is received.
- Implement a defense-in-depth privacy strategy consisting of three layers: Anonymizer, Output filter, and Image sanitizer.
- Utilize JXA (`osascript`) scripts to integrate directly with the local Apple Calendar application, bypassing Google Calendar API limitations for users who only use Apple devices.
- Build proactive features (morning brief, speaking notes) upon a persistent deduplication system using SQLite to survive server restarts.
- Design calendar routing to be profile-driven, allowing users to switch between Google, Apple, or no external calendars.
- Maintain geofrey's core values of privacy and local-first processing while improving capabilities over OpenClaw to avoid becoming a mere clone.
- Prioritize 5 high-impact improvements: 1) Wire email anonymization (critical privacy fix), 2) Timezone-aware cron scheduling, 3) Add task collection (Todoist/Things3/Reminders), 4) On-demand morning brief capability, 5) Local TTS via Piper to replace cloud ElevenLabs.
- Resolve architectural coupling by exporting a getter from `claude-code.ts` to share the anonymizer configuration with the proactive collector, avoiding duplicated logic.
- Implement conditional email anonymization where logic is only executed when `config.anonymizer` is enabled to avoid unnecessary processing overhead.
- Ensure data structures (emails, tasks) are consistently anonymized across all data collection functions (`collectMorningBriefData`, `collectNewEmails`).
- Switched default Text-to-Speech (TTS) provider from cloud to local (`piper`) to prioritize privacy and align with the 'local-first' project philosophy.
- Retained ElevenLabs as an optional fallback TTS provider and maintained backward compatibility alongside the new local solution.
- Implemented dynamic imports for task providers (Todoist, Things3, Apple Reminders) to ensure they only load when explicitly configured.
- Chose native fetch API for Todoist integration to avoid external SDK dependencies.
- Selected JXA (JavaScript for Automation) / osascript for macOS-native apps (Things 3, Apple Reminders) due to native scripting capabilities and security model.
- Established 'piper' as the default TTS provider based on documented requirements in CLAUDE.md.
- Chose a discriminated union pattern for the TTS config object to enforce strict type checking between 'piper' and 'elevenlabs' paths.
- All 5 proposed improvements were implemented, verified via the full test suite, and committed to the repository after cleaning the working tree.

## Bugs Found
- Error Handling Pattern: All tool execution flows must include a specific branch to reject absolute paths outside the project root
- Path-sensitive logic where paths matching `.env` or `/etc` triggers L2/L3 approvals automatically
- Ensure `.env` file is properly sanitized in CI/CD pipelines to prevent leaking sensitive keys like `GOOGLE_CLIENT_SECRET`
- Handle LLM-based E2E timeouts by analyzing cumulative duration vs. parent timeout, rather than relying on fixed limits for slow external tools
- Error Handling Pattern: All tool execution flows must include a specific branch to reject absolute paths outside the project root
- E2E test `src/e2e/live/risk-classifier.e2e.test.ts` timed out after 120 seconds when connecting to a real Ollama instance.
- Nested object redaction in `src/approval/risk-classifier.ts` failed to scrub sensitive data deeply nested within argument objects.
- Email anonymization existed in code but was never wired into the `sanitizeEmail()` call, causing raw PII (emails) to be exposed in generated artifacts.
- Cron expressions generated from local time caused timezone shifts, failing to run at the intended time in different regions.
- Piper CLI's raw PCM output lacked a WAV header, leading to audio playback errors when synthesized directly via `synthesizePiper()`.

## Discoveries
- Future configuration change: Enable persistent storage for reminder flags
- Database schema updated to include `proactive_reminders` table
- Application architecture allows partial functionality (using Ollama tools) when external agents (Claude Code) are unavailable
- Product differentiation often involves a trade-off between 'trust/budget' attributes (security, cost) and 'engagement' attributes (integrations, personality, proactive UX)
- Pattern of supply chain compromise: Fake crypto trading tools are used as delivery mechanisms to deploy Atomic Stealer (AMOS) malware
- Pattern of account suspension: Crash loops generate 3,500+ connection attempts within 3 hours, triggering automated 72+ hour restrictions by Meta
- Pattern of trust collapse: Rapid growth of malicious skills from 341 to 1,184+ in the marketplace indicates a lack of effective sandboxing
- Implement a 'State Manager' service that persists alert states to a database (SQLite) to prevent duplicate notifications
- Extend the Calendar Watch service to trigger a 'Preparation Service' for dynamic briefs
- Use a 'Check-Then-Act' pattern for calendar event validation and content generation
- 78% of potential users refuse cloud AI for privacy reasons, necessitating a strict local-first architecture.
- JXA (`osascript`) is required for macOS integration with Things 3 and Apple Reminders; direct Node.js HTTP calls are not feasible for these specific local applications.
- Piper CLI allows custom model paths via the `--model` flag and returns raw PCM audio via `--output_raw`, which must be manually wrapped.
- The `listVoices()` function returns an empty array for local Piper, delegating voice selection to the local file system or CLI arguments rather than an API.
- Hybrid filtering using Regex for high-confidence and LLM for low-confidence cases provides superior performance and cost-efficiency.
- Converting local time to UTC before generating cron expressions is essential for cross-timezone compatibility.
- Recursive JSON scrubbing is required to ensure deep nested object sanitization rather than shallow checks.
- Modular integration allows decoupling task providers (Google/Apple/None) based on user profile configuration.
- Background agent workflows are effective for running large test suites asynchronously without blocking the main process.
- OpenClaw's score was improved by adding Apple Calendar support (OpenClaw had no native calendar support) and increasing 3rd-party integration count from 2 to 3.
- OpenClaw's voice count was increased from 2 to 3 and its capabilities were expanded to include Todoist and Apple Calendar.
- The `anonymizer` singleton requires a configurable flag (`anonymizer.enabled`) to prevent initialization overhead when privacy processing is not active.

## Negative Knowledge
- Ensure the `setup` script explicitly warns users that geofrey does not collect telemetry to a central server, unlike open-source alternatives that log activity
- Pattern of trust collapse: Rapid growth of malicious skills from 341 to 1,184+ in the marketplace, indicating a lack of effective sandboxing
- Pattern of supply chain compromise: Fake crypto trading tools are used as delivery mechanisms to deploy Atomic Stealer (AMOS) malware
- Pattern of account suspension: Crash loops generate 3,500+ connection attempts within 3 hours, which triggers automated 72+ hour restrictions by Meta
- Testing strategy: 151 E2E tests + 1337 Unit/Integration tests
- Package manager in use: `pnpm`
- Application listens on `localhost:3003`
- Project name: `geofrey`
- API contract for `dedup` function changed from `(eventId)` to `(type, externalId)`
- Cloud-based LLM APIs are rejected by the majority of users who prioritize data privacy and local execution.
- Standard Node.js HTTP calls cannot directly trigger macOS native apps like Things 3; Apple-specific automation frameworks (JXA) are mandatory.
- Local Piper execution requires a specific .onnx model file to be present on the system path; it does not run without it.
- The `listVoices()` function does not return voice names by default for the local Piper model.

## Configuration
- To enable Telegram bot: set `PLATFORM=telegram` and inject bot token into `.env`
- Access the web interface via browser at port 3003
- Use `pnpm dev` to start the application after ensuring Ollama is running
- Implement Image Confinement via `confine()` which blocks access to restricted paths like `/etc/passwd` and `.ssh/`
- Configure prompt injection defense across 3 distinct layers: User Input, Tool Output, and Model Response isolation
- Configure all instances to utilize encrypted storage for API keys (changing default behavior)
- Configure the skill marketplace to enforce vetting beyond simple GitHub age checks to prevent Atomic Stealer malware injection
- Configure network monitoring to detect silent hijack attempts (ClawJacked) characterized by zero user interaction logs
- Privacy-First Architecture: Implementing reversible mapping tables to allow data anonymization
- Zero-Trust Filesystem: Using function confinement (`confine()`) to explicitly deny access to sensitive system files
- Configure all instances to utilize encrypted storage for API keys (changing default behavior)
- Configure the skill marketplace to enforce vetting beyond simple GitHub age checks to prevent Atomic Stealer malware injection
- Configure network monitoring to detect silent hijack attempts (ClawJacked) characterized by zero user interaction logs
- E2E test timeout configuration updated from 120_000ms to 300_000ms to accommodate slower Ollama inference endpoints.
- Sensitive argument keys (`SENSITIVE_ARG_KEYS`) are now recursively checked during `scrubArgsForLlm` operations to ensure deep sanitization.
- File `claude-code.ts`: Exported a getter function for the anonymizer config to make it globally accessible for other modules like `proactive-collector.ts`.
- File `proactive-collector.ts`: Injected `sanitizeEmail()` call into `collectMorningBriefData` function.
- File `proactive-collector.ts`: Injected `sanitizeEmail()` call into `collectNewEmails` function to ensure consistent data sanitization.
- File `config/anonymizer.ts`: Ensured the `anonymizer` singleton is initialized with a configurable flag (`anonymizer.enabled`).
- Config schema and index.ts updated to reflect Piper as the default synthesizer.
- Default TTS config set to `provider: "piper"`.
- Environment variable `TTS_PROVIDER` accepts values 'piper' (default) or 'elevenlabs'.
- Environment variable `PIPER_MODEL_PATH` is required for local Piper execution, pointing to the .onnx model file.
- Environment variable `ELEVENLABS_API_KEY` is required when switching to ElevenLabs.
- Environment variable `TTS_ENABLED` can be set to false to disable local TTS.
- Both configurations support a `CACHE_LRU_SIZE` setting to manage synthesized audio files.
- Test suite configuration verified: 6 tests skipped (expected), 0 failed.
- Repository commit hash: `e50749b`.
- Full test suite output saved to `/private/tmp/claude-501/-Users-slavkoklincov-Code-geofrey/691d7f6f-ce4a-4d2d-8002-1022eaa44014/tasks/bjve7szgk.output`.
- Piper CLI supports `--model` flag for custom model paths and `--output_raw` flag for raw PCM audio.
- The `synthesizePiper()` function requires piping text to stdin and manually wrapping raw PCM output with a WAV header using `wrapPcmInWav()`.
- The `listVoices()` function returns an empty array by default for Piper, delegating voice selection to the local file system or CLI args.
- OpenClaw comparison doc updated: Voice count (2 -> 3), Todoist availability (no -> yes), 3rd-party integration count (2 -> 3), inclusion of Apple Calendar.
- geofrey project total score adjusted to 71.
- Task provider selection routes to Todoist REST v2, Things3 JXA, or Apple Reminders JXA based on user profile configuration.
- Environment variable `TTS_PROVIDER` defaults to 'piper' if `ELEVENLABS_API_KEY` is missing.
- Config schema updated to reflect Piper as the default synthesizer, replacing or supplementing the previous default.
- Test suite configuration: 6 tests skipped (expected), 0 failed.

## Patterns
- Parallel Agent Orchestration: Using multiple sub-agents to investigate subsystems simultaneously to reduce wait times
- Incremental Testing: Running unit tests in the background while simultaneously checking for Ollama availability and E2E tests
- Status First Approach: Gathering comprehensive status reports from all agents before initiating fix implementations
- Factory pattern implemented in `create-platform.ts` using async imports
- Interface-based design for messaging platforms defined in `platform.ts`
- Throttled buffering pattern for message editing across different adapter capabilities
- Compaction Pattern: System history is pruned based on token counts when compaction triggers are hit, limiting tool result verbosity
- Use of the ToolLoopAgent pattern for streaming agent interactions
- Implementation of an Approval Gate using Promise-based blocking for critical actions
- Graceful shutdown handler pattern utilizing SIGINT/SIGTERM signals for coordinated process cleanup
- Intent classification pattern using explicit tags: QUESTION, SIMPLE_TASK, CODING_TASK, AMBIGUOUS
- Deterministic Rule: Hardcoded blocking of dangerous binaries (curl, wget, nc, ssh) regardless of tool wrapper
- Deterministic Rule: Blocking of base64 decode operations followed by execution
- Sandboxing pattern: High-risk tools (shell, git) use containerization (Docker) or strict confinement strategies before execution
- Session reuse pattern: AI tools cache session state to avoid redundant CLI invocations
- Streaming pattern: Claude Code CLI integration uses NDJSON parsing to handle large response streams
- Hybrid Risk Classification (Deterministic Rules + LLM)
- Promise-based Blocking (Approval Gate)
- Local-First Architecture
- Zod Schema Validation for Config
- WAL Database Mode for Persistence
- Compaction with Memory Management
- Streaming Responses in Orchestrator
- Smart routing pattern: Block on faces present -> OCR-only on documents/code/screenshots -> Describe on safe images
- Graceful degradation pattern for external dependencies (LLM unavailability)
- Lightweight PII extraction pattern (approx. 32 lines) focusing on specific profile feeds
- Defence-in-depth security pattern applied to privacy layer
- Streaming and compaction patterns implemented for high-volume message processing
- Email alerts use a SQLite-backed state store to track which notifications have been handled
- Calendar reminders use a SQLite-backed state store to track which notifications have been handled
- Adopt 'Static Analysis' pattern: replace runtime heuristics with local environment inspection to validate context without network calls
- Use an 'Adapter Pattern' for calendar providers: native adapter while maintaining a unified `CalendarEvent` interface
- Defense in Depth: Combining regex-based determinism with LLM checks to minimize latency while maintaining high security thresholds
- Pattern of supply chain compromise: Fake crypto trading tools are used as delivery mechanisms to deploy Atomic Stealer (AMOS) malware
- Pattern of account suspension: Crash loops generate 3,500+ connection attempts within 3 hours, which triggers automated 72+ hour restrictions by Meta
- Parallel execution of multiple distinct agent test suites using 'pnpm test'
- Scheduling with exponential backoff logic for retrying failed jobs
- Audit Logging pattern using hash-chained JSONL files to ensure immutability of logs
- Privacy Pattern: Email formatting must be sanitized before being passed to LLMs, followed by a post-processing filter for leaked credentials
- Fallback Pattern: L0-L3 risk classification should attempt deterministic matching first, falling back to LLM analysis only if necessary
- Shift from reactive user prompts to proactive AI initiation is the defining trend for 2026.
- Essential inbox management pattern: Auto-categorize/prioritize, summarize threads, draft replies, extract tasks, and overnight processing.
- Hybrid filtering: Deterministic Regex (High confidence) + LLM Fallback (Low confidence/Edge cases).
- Conditional Privacy Processing: Implementing data sanitization logic only when a specific privacy configuration flag is active prevents performance overhead when not needed.
- Aggregation Enhancement: Expanding data sources for daily briefs by integrating third-party task management APIs alongside existing calendar/email providers.
- Convert local time to UTC before generating cron expressions to ensure cross-timezone compatibility.
- Recursive JSON scrubbing for deep nested object sanitization.
- Dependency Injection for Config: Exposing internal singleton configurations (like the anonymizer) via getter functions rather than global state or hardcoding.
- Profile-driven multi-provider routing (Google/Apple/None).
- Hash-chaining for integrity verification of audit logs.
- Ensure data structures (emails, tasks) are consistently anonymized across all data collection functions.
- Architectural shift towards self-hosted/local-first tools to reduce external dependencies.
- Graceful fallback pattern: Critical privacy/anonymization functions fail open; raw data flows if anonymizer fails to prevent system paralysis.
- Modular integration pattern: Task providers are decoupled and loaded only when specific integration keys are configured.
- Timezone conversion pattern: Convert local user time to UTC before creating cron jobs for accurate scheduling regardless of server location.
- Separation of concerns: Cross-platform services handled via different strategies than native macOS services.
- Config initialization in `/src/index.ts` performs a runtime check on `config.tts.provider` to conditionally instantiate the specific synthesizer.
- Background agent workflow is effective for running large test suites asynchronously.
- Committing changes after verifying test suite success ensures code stability.
